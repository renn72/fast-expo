{
  "name": "@fast-expo/db",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "db:local": "turso dev --db-file local.db",
    "db:push": "drizzle-kit push",
    "db:generate": "drizzle-kit generate",
    "db:studio": "drizzle-kit studio",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@fast-expo/env": "workspace:*",
    "@libsql/client": "0.15.15",
    "dotenv": "catalog:",
    "drizzle-orm": "catalog:",
    "libsql": "0.5.22",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@fast-expo/config": "workspace:*",
    "drizzle-kit": "^0.31.8",
    "typescript": "catalog:"
  }
}
