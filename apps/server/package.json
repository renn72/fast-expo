{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node --import tsx dist/src/index.js"
  },
  "dependencies": {
    "@fast-expo/api": "workspace:*",
    "@fast-expo/auth": "workspace:*",
    "@fast-expo/db": "workspace:*",
    "@fast-expo/env": "workspace:*",
    "@fastify/cors": "^11.0.1",
    "@orpc/openapi": "catalog:",
    "@orpc/server": "catalog:",
    "@orpc/zod": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "fastify": "^5.3.3",
    "tsx": "^4.19.2",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@fast-expo/config": "workspace:*",
    "@types/node": "^22.13.14",
    "tsdown": "^0.16.5",
    "typescript": "catalog:"
  }
}
